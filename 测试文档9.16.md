# 测试文档9.16

本文档意在通过实际测试，确定程序中存在的问题，并尝试分析可能的改进方向。

## 程序维护

**1.缺少注释，需要对每个模块的代码都加上注释。**

2.需要完善一张开发思路表

## GUI 界面

**1.保存参数功能调用过于反人类，把从两个按钮之间的缝处调用改为按钮调用。**

**2.欢迎界面过于混沌，需要修改并介绍对应功能。**

**3.进度条逻辑问题**

4.参数输入优化，设定为“普通参数”和“高级参数”

## 体验优化

**1.货架组合太多，严重拖慢运行速度**

**2.图形化界面和算法没有分开运行**，**导致卡顿**

3.图表存在逻辑问题，且不能很好地表现出合理性。

## 算法优化

1.**算法繁多冗杂，有必要清除无用算法**

2.使用Dijkstra算法进行优化，提高精准度和运算速度

````c++
#include <iostream>
#include <cstring>
#include <string>
using namespace std;

struct Student{
    
    int id;
    char name[9];
    int grade;
    
}
void jiaohuan(Student s[],int N){
   int max=s[0].grade;
   int min=s[0].grade;
   int i;
   int j;
   for(i=0;i<N;i++)
   {
           if (max < s[i].grade)
           {int max;
               max = s[i].grade;
           maxcount=i};
   }
    for(j=0;j<N;j++)
    {
       	   if (min > s[j].grade)
           {int min;
               min = s[j].grade;
           mincount=j};
   }
	Student temp;
    temp = s[maxcount];
    s[maxcount] = s[mincount];
    s[mincount] = temp;
}
    
````

